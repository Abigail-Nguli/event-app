{
  "name": "json-server-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT /opt/render/project/src/db.json",
    "prestart": "ls -la /opt/render/project/src/"
  },
  "dependencies": {
    "json-server": "^1.0.0"
  }
}
