{
  "name": "json-server-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "prestart": "node -e \"const fs = require('fs'); if (!fs.existsSync('db.json')) { console.error('ERROR: db.json missing!'); process.exit(1); }\""
  },
  "dependencies": {
    "json-server": "^1.0.0"
  }
}
